<?xml version="1.0" encoding="utf-8"?><!--
   Legacy pre-Android-12 backup rules (kept in sync with data_extraction_rules.xml's excludes —
   see that file for the rationale). Only relevant if minSdk ever drops below 31; on this app's
   current minSdk (31), data_extraction_rules.xml governs instead.
   See https://developer.android.com/guide/topics/data/autobackup
-->
<full-backup-content>
    <exclude domain="database" path="."/>
    <exclude domain="file" path="datastore/push_state.preferences_pb"/>
</full-backup-content>
